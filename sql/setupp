
/*
====================================================
PROJECT: Moroccan E-Commerce Analysis
FILE: 01_setup.sql
PURPOSE: Database creation and initial data setup
====================================================
*/


-- ==================================================
-- STEP 1: Create the project database
-- ==================================================

CREATE DATABASE Moroccan_Ecommerce;


-- ==================================================
-- STEP 2: Switch to the project database
-- ==================================================

USE Moroccan_Ecommerce;


-- ==================================================
-- STEP 3: Import the source Excel files
-- ==================================================
-- The Orders and Details Excel files were imported
-- into SQL Server using the SQL Server Import Wizard.
--
-- During the import process:

-- Source columns and data types were reviewed. 
-- NULL values were checked in the imported tables.

-- The following tables were created:
-- 1. orders_morocco
-- 2. details_morocco


-- ==================================================
-- STEP 4: Inspect the Orders table structure
-- ==================================================

SELECT
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'orders morocco dataset'
ORDER BY ORDINAL_POSITION;


-- ==================================================
-- STEP 5: Inspect the Details table structure
-- ==================================================

SELECT
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'details morocco dataset'
ORDER BY ORDINAL_POSITION;

-- ==================================================
-- STEP 6: Check imported row counts
-- ==================================================

SELECT COUNT(*) AS Orders_Row_Count
FROM [orders morocco dataset];

SELECT COUNT(*) AS Details_Row_Count
FROM [details morocco dataset];
